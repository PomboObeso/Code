#include <stdio.h>
#include <math.h>
#include <stdlib.h>


int main()
{
  int lado1 = 0;
  int lado2 = 0;

  scanf("%d", &lado1);
  scanf("%d", &lado2);

  int area = (lado1 * lado2);

  printf("%d\n", area);
  return 0;
}
