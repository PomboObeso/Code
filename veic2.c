#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
  int dias = 0;
  int km = 0;

  scanf("%d", &dias);
  scanf("%d", &km);

  double diaria = 90 * dias;
  double total = 0;
  double exc = 12.0;

  if(km > 100){
     
  }

  printf("%.0lf\n",diaria);

  return 0;
}
